//declaration a variable
let a=10 ;
// getting the main div from body 
const elem = document.getElementById("id01");

//creating the heading1
const add_Heading1 = document.createElement("h1");
const heading1_Data = document.createTextNode(
    `Value of a is: ${ a }`
    );
add_Heading1.appendChild(heading1_Data);

//appending the heading in main div
elem.appendChild(add_Heading1);
// ===========================================================================================================
//creating the heading2
const add_Heading2 = document.createElement("h1");
const heading2_Data = document.createTextNode(
    `==============================================`
    );
add_Heading2.appendChild(heading2_Data);

//appending the heading in main div
elem.appendChild(add_Heading2);

// ===========================================================================================================
//creating the heading3
const add_Heading3 = document.createElement("h1");
const heading3_Data = document.createTextNode(
    `Value of ++a is: ${ ++a }`
    );
add_Heading3.appendChild(heading3_Data);

//appending the heading in main div
elem.appendChild(add_Heading3);

// ===========================================================================================================

//creating the headingn
const add_Headingn = document.createElement("h1");
const headingn_Data = document.createTextNode(
    `Value of a is: ${ a }`
    );
add_Headingn.appendChild(headingn_Data);

//appending the heading in main div
elem.appendChild(add_Headingn);

// ===========================================================================================================
//creating the heading5
const add_Heading5 = document.createElement("h1");
const heading5_Data = document.createTextNode(
    `Value of a++ is: ${ a++ }`
    );
add_Heading5.appendChild(heading5_Data);

//appending the heading in main div
elem.appendChild(add_Heading5);



// ===========================================================================================================
//creating the heading6
const add_Heading6 = document.createElement("h1");
const heading6_Data = document.createTextNode(
    `Value of a is: ${ a }`
    );
add_Heading6.appendChild(heading6_Data);

//appending the heading in main div
elem.appendChild(add_Heading6);

// ===========================================================================================================
//creating the heading7
const add_Heading7 = document.createElement("h1");
const heading7_Data = document.createTextNode(
    `Value of --a is: ${ --a }`
    );
add_Heading7.appendChild(heading6_Data);

//appending the heading in main div
elem.appendChild(add_Heading7);


//creating the heading8
const add_Heading8 = document.createElement("h1");
const heading8_Data = document.createTextNode(
    `Value of a is: ${ a }`
    );
add_Heading8.appendChild(heading7_Data);

//appending the heading in main div
elem.appendChild(add_Heading8);

// ===========================================================================================================
//creating the heading9
const add_Heading9 = document.createElement("h1");
const heading9_Data = document.createTextNode(
    `Value of a-- is: ${ a-- }`
    );
add_Heading9.appendChild(heading5_Data);

//appending the heading in main div
elem.appendChild(add_Heading9);

// ===========================================================================================================

//creating the heading4
const add_Heading4 = document.createElement("h1");
const heading4_Data = document.createTextNode(
    `Value of a is: ${ a }`
    );
add_Heading4.appendChild(heading4_Data);

//appending the heading in main div
elem.appendChild(add_Heading4);





//appending the heading in main div

